[package]
name = "rway-ipc"
version = "0.1.0"
description = "Sway-compatible IPC protocol implementation for rway compositor"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
calloop = "0.14"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
